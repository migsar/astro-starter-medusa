---
import { Image } from '@astrojs/image/components';

const DEFAULT_WIDTH = 480;

const { src, alt, width: rawWidth } = Astro.props;
const width = rawWidth || DEFAULT_WIDTH;
---
<Image src={src} width={DEFAULT_WIDTH} aspectRatio={1} format="png" alt={alt} />